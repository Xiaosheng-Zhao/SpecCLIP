seed_everything: 42
trainer:
  default_root_dir: "{SPECCLIP_ROOT}/outputs"
  enable_checkpointing: true
  gradient_clip_val: 1.
  max_steps: 122500 
  precision: 32 
  limit_val_batches: 100
  accelerator: 'gpu' 
  devices: 8 
  strategy: 'ddp'
  num_nodes: 8
  logger:
    class_path: CustomWandbLogger
    init_args:
      project: "gaia-xp-mt"
      entity: "{WANDB_ENTITY_NAME}"
      save_dir: ${trainer.default_root_dir}
  callbacks:
    - class_path: LearningRateMonitor
      init_args:
        logging_interval: "step"
    - class_path: ModelCheckpoint
      init_args:
        save_last: True #link
        save_top_k: 1
        monitor: "val_epoch_loss"
        mode: "min"
        filename: "{epoch}-{val_loss:.4f}"
    - class_path: CustomEarlyStopping
      init_args:
        monitor: "val_epoch_loss"
        min_delta: 1e-5
        patience: 20
        verbose: False
        mode: "min"
        check_finite: True
    - class_path: EpochLossCallback
      init_args:
        log_train_path: "{SPECCLIP_ROOT}/outputs/training_loss_xp_mt.txt"
        log_val_path: "{SPECCLIP_ROOT}/outputs/validation_loss_xp_mt.txt"
model:
  class_path: SpecFormerControl1_stats
  init_args:
    input_dim: 1
    embed_dim: 768
    num_layers: 6 
    num_heads: 6 
    max_len: 350
    dropout: 0.
    mask_num_chunks: 6 
    mask_chunk_width: 20 
    slice_section_length: 20 
    slice_overlap: 10 
    include_stats: True
data:
  class_path: SpectrumDataloader
  init_args:
    path: "{SPECCLIP_ROOT}/data/gaia_spectra_subsample_100.h5"
    batch_size: 64
    num_workers: 32
optimizer:
  class_path: torch.optim.adamw.AdamW
  init_args:
    lr: 1e-5 
    weight_decay: 1e-1 
    betas:
      - 0.9
      - 0.95
lr_scheduler:
  class_path: CosineAnnealingWithWarmupLR
  init_args:
    T_max: ${trainer.max_steps}
    T_warmup: 1220 
    eta_min: ${eval:'${optimizer.init_args.lr}//100'}
